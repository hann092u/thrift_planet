---
const { image, animation, title, bg } = Astro.props;
import Menu from "../components/Menu.astro";
---

<header>
  <div class="menu"><Menu /></div>
  <div class="baggrund"><slot /></div>

  <div class="container_text">
    <img class="size" src={image} alt="" />
    <h1 class="padding_top">{title}</h1>
  </div>
  <div class="container_animation">
    <a href="/webshop"> <img class="animation_size" src={animation} alt="" /></a>
  </div>
  <div class="lottie">
    <script src="https://unpkg.com/@lottiefiles/lottie-player@latest/dist/lottie-player.js"></script>
    <lottie-player class="sizelottie" src="/img/general/arrow.json" background="transparent" speed="1" style="width: ; height: 200px;" loop autoplay></lottie-player>
  </div>
</header>

<style>
  .baggrund {
    grid-row: 1/5;
    grid-column: 1/6;
  }

  header {
    display: grid;
    grid-template-columns: 0.5fr 1fr 0.5fr 1fr 0.5fr;
    grid-template-rows: 0.5fr 0.7fr 1fr 1fr;
    background-color: var(--bg);
  }

  .menu {
    grid-row: 1;
    grid-column: 5;
    z-index: 5;
  }

  .container_text {
    grid-row: 3;
    grid-column: 1/6;
  }
  .container_animation {
    grid-row: 2;
    grid-column: 4/5;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  img {
    width: 100%;
  }

  .lottie {
    grid-column: 3;
    grid-row: 4;
    display: flex;
    flex-direction: column;
    justify-content: left;
    margin-top: 20px;
  }

  .animation_size {
    width: 100%;
    animation: cirkelAnimation 10s infinite;
  }

  @keyframes cirkelAnimation {
    0% {
      rotate: 0;
    }

    50% {
      rotate: 180deg;
    }

    100% {
      rotate: 360deg;
    }
  }

  @media (min-width: 800px) {
    .baggrund {
      grid-row: 1/4;
      grid-column: 1/5;
    }

    header {
      display: grid;
      grid-template-columns: 0.5fr 4fr 2fr 0.5fr;
      grid-template-rows: 0.2fr 1.5fr 1fr;
      background-color: var(--bg);
    }

    .menu {
      grid-row: 1/2;
      grid-column: 2/4;
      z-index: 5;
    }

    .container_text {
      grid-row: 2/4;
      grid-column: 2/3;
    }
    .container_animation {
      grid-row: 2;
      grid-column: 3/4;
    }
    .container_text {
      width: 100%;
      padding-bottom: 20px;
    }
    .size {
      width: 95%;
    }
    .animation_size {
      width: 100%;
      padding: 15px;
    }

    .lottie {
      grid-column: 3;
      grid-row: 3;
    }

    h1 {
      padding-top: 400px;
    }
  }
</style>
